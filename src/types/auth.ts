export interface RegisterTypes {
  username: string;
  login: string;
  password: string;
  confirm: string;
  email: string;
  phone?: string;
}

export interface LoginTypes {
  login: string;
  password: string;
}
